<Window x:Class="MissionBossAot.Views.TurnHeadQrCodePrint"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MissionBossAot.Views"
        mc:Ignorable="d"
        Title="转头二维码打印" Height="900" Width="1200" ShowInTaskbar="False"
        WindowStyle="None"
        WindowState="Maximized"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="White"
        WindowStartupLocation="CenterScreen"
        Name="TurnHeadQrCodeForm">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏区域 -->
        <Border Grid.Row="0" Background="#2C3E50" Height="50" CornerRadius="5">
            <Grid>
                <TextBlock Text="转头二维码打印" FontSize="24" FontWeight="Bold" Foreground="White" 
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button Content="✕" Width="40" Height="35" HorizontalAlignment="Right" VerticalAlignment="Center" 
                Margin="0,0,10,0" Click="CloseButton_Click" FontSize="18" 
                Background="#E74C3C" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- 按钮区域 -->
        <Border Grid.Row="1" Background="#F8F9FA" CornerRadius="5" Margin="0,10,0,10" Padding="20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button x:Name="PrintLandingGearBtn" 
                        Content="打印起落架转头标签" 
                        Click="PrintLandingGearBtn_Click"
                        Width="200" 
                        Height="60" 
                        Margin="10"
                        FontSize="16" 
                        FontWeight="Bold"
                        Background="#3498DB" 
                        Foreground="White" 
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button x:Name="PrintRobotArmBtn" 
                        Content="打印机械臂转头标签" 
                        Click="PrintRobotArmBtn_Click"
                        Width="200" 
                        Height="60" 
                        Margin="10"
                        FontSize="16" 
                        FontWeight="Bold"
                        Background="#2ECC71" 
                        Foreground="White" 
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <!-- 内容区域（预留用于显示预览或其他内容） -->
        <Border Grid.Row="2" Background="#FFFFFF" CornerRadius="5" BorderBrush="#E0E0E0" BorderThickness="1">
            <Grid>
                <!-- 这里可以放置二维码预览区域或其他内容 -->
                <TextBlock Text="" 
                           FontSize="16" 
                           Foreground="#999999" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>